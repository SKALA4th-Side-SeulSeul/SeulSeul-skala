# 현재 작업과 남은 검증

완료된 구현·검증 이력은 [HISTORY.md](HISTORY.md)에 보존합니다. 이 문서는 현재 진행·미검증 항목만 관리합니다. `구현`, `자동 검증`, `사용자 확인`, `운영 직접 확인`을 구분하고 확인되지 않은 배포 상태를 완료로 추정하지 않습니다.

## 운영 검증

- [ ] 서버 자동 백업 예약 등록 후 성공 로그·파일 확인
- [ ] 격리 DB 복구 시험과 서버 밖 백업 보관 적용
- [ ] 백업 보존 기간·디스크 용량 점검·장애 알림 정책 확정
- [ ] 실제 DB 포트·SSH 인증/포워딩·조회 전용 계정 권한 확인 및 저장소와 서버 설정 일치 ([DB-ACCESS.md](DB-ACCESS.md))
- [ ] 운영 Alembic revision 직접 확인 (`d94132ac684e`), 단일 봇 프로세스·재부팅 후 복구 확인
- [ ] 운영 메모리 측정, 클라우드 방화벽·유휴 회수 정책 확인 ([DB-TODO.md](DB-TODO.md))

## 제품·개발 검증

- [ ] 두 개발자 환경의 설치와 `./scripts/check.sh` 통과 확인
- [ ] 실제 한국어 공지 표본으로 Qwen3 로컬 분석 결과 검증
- [ ] Qwen3 로컬 처리와 개인정보 처리 정책 확정
- [ ] Oracle 운영 봇 컨테이너에서 Ollama `/api/chat` 접근 및 `think=false` 실제 처리 확인
- [ ] 이번 점검에서 추가한 동작을 운영에 반영하고 실제 Slack에서 재확인
- [ ] 버튼 검증 완화 복원(현재 본인 메시지의 ID·채널·ts 정확한 일치)을 운영에 반영한 뒤 완료·취소·목록 전환 확인. 기존 반복 오류는 로컬 봇 종료 후 정상 동작을 사용자 확인했으며, 이번 복원 코드의 배포와는 별개임 ([README 로그 절차](../README.md#체크리스트-버튼-오류-로그-확인))
- [ ] 버튼 응답의 `replace_original=false`, `delete_original=false` 보완을 배포하고, 오류 안내 뒤에도 체크리스트가 유지되는지 실제 Slack에서 확인.
- [ ] 일시 장애 자동 재처리의 예약·복구·3회 소진을 운영에서 확인 (D-030, 스키마 변경 없음)
- [ ] 운영 Slack 앱에 `user_change` 구독 추가 후 정상 성명의 3반→1반·2반→4반 변경이 같은 DM에 반영되는지 확인 (D-031 구현 완료, 실제 서버·Slack 적용 별도)
- [ ] 격리 PostgreSQL의 잠금·길이 제한·마이그레이션 통합 테스트 추가
- [ ] 이번 요청 범위에서 제외한 설정 오류·AI 서버 오류 본문의 비밀값 로그 노출 방지
- [ ] 수동 공지 CLI를 운영 서버에 반영하고 전체공지 등록·수정·삭제 및 AI 실패 수동 대체값을 실제 Slack 체크리스트에서 확인
- [ ] `notice.sh` 통합 운영 도우미를 운영 이미지에 반영하고 등록·수정·삭제 및 AI 실패 링크별 수동 입력, 같은 DM 갱신을 실제 Slack에서 확인. DB·권한·manifest 변경 없음.
- [ ] 상대 마감일 수정 시 기준 시각 및 식별용 쿼리 제거 정책 검토 (현재 정책은 유지)

## 사용자 확인 완료와 미확인 경계

운영 배포·DB 연결·DBeaver 접속 및 시작/등록/완료/수정/삭제/반별 동일 링크 흐름은 사용자가 확인했습니다. 기존 정상 흐름을 미구현으로 다시 분류하지 않습니다. 자동 백업·복구·권한·이번 수정의 배포는 별도 검증 항목입니다.

2026-09-21 반복되던 버튼 소유권 오류는 로컬 봇 두 개를 종료한 뒤 해결됐다고 사용자가 확인했습니다. 진단 당시의 원인 미확정 기록은 HISTORY에 보존하며, 검증 완화 복원과 원본 보존 보완의 운영 반영은 위 항목으로 따로 관리합니다.

## 현재 의도적으로 제공하지 않는 기능

- 누락 Slack 이벤트의 기록 전체 자동 수집·영속 작업 큐: 현재 원문 수정/재게시 또는 운영자 재처리 절차를 사용합니다.
- 공지마다 모든 학생의 Slack 성명을 다시 조회: API 호출량·지연을 늘리므로 하지 않습니다. `user_change` 이벤트가 변경된 가입자만 동기화하며 `/seulseul 시작`도 수동 복구 수단으로 유지합니다.
- 실제 폼 제출 여부 자동 확인: 완료 버튼은 학생이 직접 누릅니다.
- 일일 신규 체크리스트 DM: 최초 메시지만 계속 갱신합니다.
