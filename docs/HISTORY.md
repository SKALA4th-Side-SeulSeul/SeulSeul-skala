# 구현·검증 이력

## 2026-09-22 금일 마감 표현 검증 보완

- 날짜 검증기가 `금일`을 인식하지 못해 올바른 AI 응답도 거절하는 문제를 재현하고, 최초 게시일의 한국 날짜로 해석하도록 수정했습니다. AI 지시에도 같은 기준을 명시했습니다.
- 제출 마감 13:10과 별도 발표 13:30이 함께 있는 원문, AI 근거의 오늘·명시 날짜 표현, UTC 날짜 경계, 시각 생략 및 잘못된 날짜·시간 거절을 테스트합니다. 운영 배포·기존 실패 데이터 재처리는 별도입니다.
- 검증: `./scripts/check.sh` 통과(578개 테스트, Ruff lint·format). 실제 AI·Slack 호출 및 운영 DB 변경 없음.

## 2026-09-22 가입 학생 운영 안내 DM

- `./announce.sh`에 안내문 입력·인원/내용 확인·수동 발송을 추가했습니다. 토큰의 워크스페이스에 가입한 학생만 조회하고 발송 직전 해지 여부를 다시 확인합니다.
- 별도 일반 DM을 순차 발송하며 성공·실패·결과 불명·해지 제외·미발송 건수를 표시합니다. 예약·자동 재시도·영속 발송 기록은 제공하지 않습니다. DB·manifest 변경 없이 실행 중인 DB와 빌드된 봇 이미지를 사용하며 추가 Socket Mode 프로세스를 띄우지 않습니다.
- 실제 학생 발송은 수행하지 않았습니다. 운영 반영과 발송 확인은 PLANS에서 관리합니다.
- 입력 완료는 빈 줄에서 Ctrl+D, 취소는 Ctrl+C로 변경했습니다. Docker TTY를 연결하여 입력 완료 뒤에도 미리보기의 발송 확인을 받을 수 있습니다. 확인 화면의 Enter·Ctrl+D도 취소입니다.
- 검증: `./scripts/check.sh` 통과(572개 테스트, Ruff lint·format), 셸 문법 및 PTY에서 Ctrl+D → 미리보기 → y → 모의 발송 확인.

완료된 구현·검증 기록을 보존합니다. 과거 테스트 수·화면 설명은 당시 상태이며 최신 제품 명세가 아닙니다. 남은 작업은 [PLANS.md](PLANS.md), 현재 동작은 [PRODUCT.md](PRODUCT.md), 정책 변경은 [DECISIONS.md](DECISIONS.md)를 따릅니다.

## 2026-09-21 운영 공지 간편 수정 스크립트

- `./notice_edit.sh`에서 최근 공지를 번호로 선택하고 제목·요약·마감일·근거 중 바꿀 값만 입력하도록 추가했습니다. Enter는 기존 값 유지, 마지막 `y` 확인 후 저장하며 기존 값이 없는 AI 실패 공지도 수동 복구합니다.
- 워크스페이스·원문은 DB에서 재사용하고 기존 수동 수정 서비스를 호출합니다. 원문·제출 링크·완료 기록은 보존하며 AI 호출·임시 원문 파일·추가 Socket Mode 봇 실행은 없습니다. 기존 등록·수정·삭제 CLI도 유지합니다.
- 단일 링크 원문만 지원하며 저장 직전 원본 잠금 아래 선택 당시 기록을 비교합니다. 다른 작업의 변경·삭제·재처리 또는 처리 중인 원본을 발견하면 저장을 중단합니다. DB 구조·manifest·환경변수 변경은 없습니다.
- 자동 검증: `./scripts/check.sh`의 536개 테스트·린트·포맷 검사 통과. 입력 취소·검증·경합 방어·AI 실패 복구·운영 스크립트 경계·완료 기록 보존과 동일 DM 갱신을 확인했습니다. 두 도움말 명령과 셸 문법도 확인했으며 실제 운영 DB 변경·Slack 호출·배포는 수행하지 않았습니다.

## 2026-09-21 통합 운영 공지 콘솔과 다중 링크 구분

- `./notice.sh`에서 공지 등록·수정·삭제를 메뉴로 선택하도록 추가했습니다. 등록은 원문 permalink·멀티라인 원문·AI 분석 또는 링크별 수동 분석을 받고, 수정·삭제는 기존 서비스와 완료 기록 보존 정책을 재사용합니다. 기존 `notice_edit.sh`와 manual CLI는 유지합니다.
- 같은 Slack 원문에 링크가 여러 개면 수정 목록에 제출 링크를 표시하고 체크리스트에는 `[순번/전체]`와 짧은 호스트·경로를 표시합니다. 수정은 선택한 링크만 변경하며 삭제는 실제 원문 삭제 의미에 따라 해당 원문의 모든 링크를 삭제합니다.
- 자동 검증: 수동 등록·삭제·다중 링크별 분석, 선택 링크 경합 방어, 화면 식별자, 운영 셸 경계를 포함한 테스트를 추가했습니다. 실제 운영 DB·Slack 호출·배포는 수행하지 않았습니다.

## 2026-09-21 버튼 오류 원인 확인과 검증 완화 복원

- 사용자 확인: 로컬 봇 두 개를 종료한 뒤 반복되던 버튼 소유권 오류가 사라지고 정상 동작했습니다. 아래 조사 당시의 원인 미확정 상태를 갱신하는 후속 확인입니다.
- 사용자 요청에 따라 원인 조사 중 추가한 주소 기반 발송 기록 ID 보정·과거의 다른 메시지 버튼 허용·ts 숫자 비교를 제거했습니다. 현재 사용 중인 본인 기록의 ID·채널·ts 정확한 일치 검증으로 복원하고, 잘못된 ID로 모든 버튼을 누르는 회귀 사례를 보강했습니다.
- 진단 로그·지정 기록 소실 시 새 DM 발송 방지·버튼 안내의 원본 보존 옵션은 유지합니다. DB·manifest·운영 서버는 변경하지 않았으며 복원 코드의 실제 배포 확인은 PLANS에 남깁니다.
- 자동 검증: 복원 전 24개 거부 사례의 실패를 확인한 뒤 수정했으며, `./scripts/check.sh`의 501개 테스트·린트·포맷 검사를 통과했습니다.

## 2026-09-21 체크리스트 버튼 진단 로그

- 버튼 수신·값 파싱·발송 기록 검증·결과를 `trace_id`로 연결하는 JSON 로그 추가. 기록 없음·소유권·채널·메시지 시각 불일치와 주소 보정 성공을 구분하고, 최초 발송 주소 저장/발송권 충돌도 기록합니다. Slack 식별자는 해시 참조로 남기며 전체 payload·비밀값·예외 본문은 제외합니다.
- 커밋 전 검토에서 지정된 발송 기록이 사라진 경우 새 DM을 만들지 않도록 보완하고 회귀 테스트를 추가했습니다.
- 자동 검증: `./scripts/check.sh`의 477개 테스트·린트·포맷 통과. 실제 Slack에서 반복되는 오류의 원인은 아직 확정하지 않았으며 수정 코드 반영과 재현 로그 수집은 PLANS의 미검증 항목입니다.

## 2026-09-21 버튼 안내의 원본 메시지 보존

- 버튼 오류·갱신 대기 응답에 `replace_original=false`, `delete_original=false`를 명시했습니다. 실제 Bolt 응답 직렬화를 사용하고 네트워크만 대체하는 회귀 테스트로 거부·잘못된 입력·예상 밖 오류·갱신 대기 네 경로의 원본 보존 옵션을 확인했습니다.
- 자동 검증: `./scripts/check.sh`의 481개 테스트·린트·포맷 통과. 전달받은 운영 로그로는 최초 발송 기록 저장 성공만 확인했으며, 버튼 소유권 검증 실패와 실제 화면 소실의 원인은 아직 확정하지 않았습니다.
- 조사 중 같은 프로젝트 폴더의 로컬 봇 두 개가 실행 중인 것을 확인하고 사용자 승인으로 정상 종료 신호를 보냈습니다. 운영과 같은 Slack 앱인지·같은 DB인지까지 확인한 것은 아니며, 단일 운영 봇으로 버튼 재현과 응답 보완 배포 확인이 남아 있습니다.

## 이후 확인된 운영 상태

- D-032: 봇 미초대 채널을 위한 운영자 수동 공지 CLI(`add`·`edit`·`delete`)와 AI 실패 시 제목·요약·마감일 수동 대체값 구현. 자동 테스트·운영 적용은 PLANS에서 별도 확인합니다.

- 사용자 확인: 운영 서버 배포·DB 연결 및 DBeaver 접속 성공. 15432·기존 비밀번호 SSH 안내 후 성공한 것이며 실제 SSH 계정과 조회 전용 DB 권한은 아직 검증하지 않았습니다.
- 사용자 확인: 시작·공지 등록·완료·공지 수정/삭제·같은 링크의 1반/3반 배정과 한쪽 삭제 후 다른 반 유지 흐름을 확인했습니다. 자동 테스트 결과와 별개의 사용자 확인 기록입니다.
- 위 확인은 자동 백업 예약 실행, 격리 DB 복구, 외부 보관, 재부팅 복구, NVIDIA 표본 정확도 평가까지 완료했다는 뜻은 아닙니다. 운영 Alembic revision을 직접 조회한 증거도 별도로 확인합니다.

## 2026-09-20 점검 후 보완

- D-031: 승인된 `user_change` 구독과 가입 학생의 현재 성명 재조회·조건부 반 갱신·동일 DM/완료 기록 보존 구현. 전체 자동 검증 445개 테스트·린트·포맷 통과. 앱 이벤트 구독과 실제 운영 확인은 별도입니다.

- D-030: 기존 DB 컬럼으로 일시 장애 지연 자동 재처리, 5분·15분·1시간 최대 3회·예약 선반영·별도 작업·원본 경합 보호 구현. 성명 변경 이벤트 방식은 설계 검토만 진행했습니다. 전체 자동 검증 426개 테스트·린트·포맷 통과.

- D-028: 현재 운영 상태와 문서 기준 정리, Docker 운영 재처리 명령 추가, 안전 배포 절차 통일, DB 접속 설정 차이 명시.
- D-029: 성명 형식 오류 일반 DM, AI 결과·원문 날짜 독립 검증과 재시도 대기 상한, 예외·원문 링크 조회 실패 보관 및 선택 복구, 미적용 원본 조회, 예약 백업 호스트 실행 상한.
- 자동 검증: `./scripts/check.sh`의 테스트 411개·린트·포맷 통과. 운영 반영·실제 확인은 PLANS의 잔여 작업으로 유지합니다. 이 기록만으로 서버에 배포됐다는 뜻은 아닙니다.

## 완료된 작업

### 1. 개발 기반

- [x] `COLLABORATION.md`를 기준 문서 링크 구조로 정리하고 `docs/DECISIONS.md`와의 충돌 해소

## 2. 공지 수집과 AI 분석

- [x] D-027: 채널별 동일 링크 수집·원본별 분석, 학생별 링크 중복 제거와 반 원본 우선 선택, 완료 기록 보존, 선택 재처리 원본 지정, 마이그레이션 작성 및 격리 PostgreSQL 검증

- [x] 공지 수집·대상 판별·AI 결과·중복 처리 정책 확정 (D-009~D-012)
- [x] 링크 단위 처리·마감일 해석·DB·AI 호출 정책 확정 (D-014~D-017)
- [x] 설정된 Slack 채널 ID의 최상위 메시지만 처리
- [x] `form`·`docs` URL 추출과 링크별 공지 생성
- [x] 원본 URL 보존, canonical URL 정규화와 메모리 중복 방지
- [x] AI JSON 결과의 필수 필드·마감일·원문 표현 검증
- [x] 12,000자 입력 제한, 45초 timeout, 최대 3회 재시도 적용
- [x] `processing_failed` 공지 목록 조회와 선택 재처리 (CX: 운영자 CLI·서비스, D-022)
- [x] 원본 메시지 수정 시 링크·분석 결과 동기화 (CX: D-024, 실패 시 기존 분석·완료 상태 보존)
- [x] 원본 메시지 삭제 시 공지·체크리스트 삭제 상태 전환 (CX: 개인 DM 갱신)
- [x] 원본별 이벤트 버전 저장과 중복·역순·분석 중 삭제 방어, 마이그레이션·격리 PostgreSQL 검증 (CX)

## 3. Slack 연동과 사용자 화면

- [x] Slack 링크 탭을 참고한 카드형 목록 정리 (CX: 굵은 제목·작은 도메인/마감 메타 정보·큰 달력 제거·하단 목록 버튼, 테스트 294개·실제 Slack 컨테이너 카드 표시 확인, 미지원 환경의 명시적 거절에만 호환 레이아웃 적용)
- [x] 최초 연동 시 한 번 발송하고 최초 DM을 계속 갱신, 짧은 행·문서/폼 아이콘·완료 아이콘 버튼 적용 (CX: D-025, 날짜 변경·재시작·기존 기록·권한·재시도 테스트 288개 및 실제 기존 Slack DM 갱신 확인; manifest 정시 발송 설명 제거)
- [x] Socket Mode 사용 확정 (D-018)
- [x] Slack 원문 permalink 조회와 원문 전체 미노출 (D-019)
- [x] SeulSeul 자신이 작성한 메시지만 제외
- [x] 실제 Slack API를 호출하지 않는 신규 메시지 handler 테스트
- [x] 실제 명령어 이름과 가입·알림 해지 흐름 확정
- [x] Slack 성명(`profile.real_name`)에서 캠퍼스·반 판별 및 잘못된 성명 형식 안내
- [x] Slack 명령 응답 전송을 `slack/client.py`로 이동하고 수신 확인·응답 경계 테스트 추가
- [x] 학생별 체크리스트 조회·완료·완료 취소 화면 (CX: D-023 배정·페이지·필터·소유권 검증, 메시지 수명은 D-025로 대체)
- [x] 체크리스트 표시 정리 (CX: 제목 상태 접두어 제거·인용구, 📅 마감, 마지막 갱신 문구; 기존 버튼·목록 유지, 테스트 255개·실제 Slack 화면 확인)
- [x] 목록 개수·마지막 갱신 날짜와 한국어 요일을 상단에 통합하고 하단 제출 안내·갱신 문구 제거
- [x] 두 공지 중 하나만 보이는 문제 점검 (CX: 수집 누락이 아닌 완료 필터로 확인, 현재 목록·전환 개수 안내, 서로 다른 링크 수집·배정·목록 전환 테스트 및 실제 Slack 표시 확인)
- [x] 날짜 제목으로 인용구 이동·각 할 일 제목에 글머리 기호(*) 적용 (CX: 테스트 262개·실제 Slack 화면 확인, 현재 목록 구분 UI의 추가 변경은 아이디어 제안만 진행)
- [x] 글머리를 실제 동그라미 목록 서식으로 정정하고 제안한 두 가지 목록 구분 UI 적용 (CX: 테스트 265개·실제 Slack 화면 확인; 큰 목록 소제목·두 버튼 상시 표시/선택 강조는 후속 요청으로 롤백)
- [x] 할 일 제목에 제출 링크 연결·마감일을 Slack 원문 링크 앞으로 이동·두 가지 목록 구분 UI 롤백 (CX: 동그라미·날짜 인용구 유지, 단일 전환 버튼·작은 목록 안내 복원, 테스트 266개·실제 Slack 화면 확인)
- [x] 학생 명령을 시작·해지로 제한; 후속 요청으로 슬래시 명령의 모든 임시 응답 제거, 오류 로그 유지
- [x] 해지 결과는 일반 DM으로 안내하고 시작 시 체크리스트만 발송; 중복 구버전 봇 실행 여부 확인
- [x] 수정·삭제 이벤트 payload 검증과 서비스 연결 (CX: 원본 ts·채널·스레드·봇 검증)
- [x] `slack-manifest.yaml`에 명령어·이벤트·OAuth scope 기록

## 4. 저장과 알림 — DB 연결 시 진행

- [x] SQLAlchemy·Alembic·Psycopg 3 의존성 추가
- [x] PostgreSQL 연결과 공통 세션 구성
- [x] 학생·공지·체크리스트 모델과 첫 마이그레이션 작성
- [x] 공지 처리 상태·재시도 횟수·최근 오류 저장
- [x] 원본 삭제의 soft delete 이벤트 연결 (CX: 공지·체크리스트 원자적 갱신)
- [x] 알림 해지 시 학생·체크리스트·개인 DM 발송 기록 DB 삭제
- [x] 시작·해지 시 본인 DM의 기존 봇 메시지 정리, 시작 시 완료 기록 보존·발송 기록 초기화, 실패 시 갱신 중지 및 명령 재시도 (D-026; im:history 반영·앱 재설치 필요)
- [x] 체크리스트 배정과 완료 상태 변경 (CX)
- [x] 개인 DM 발송 시점과 완료 항목 제외 정책 확정 (D-025: 정시 발송 제거)
- [x] 알림 재시도와 중복 발송 방지 (CX: 첫 전송 결과 불명확 시 자동 재발송 중지, 오전 9시/당일 제한은 D-025에서 제거)
- [x] 일일 DM 발송 기록 마이그레이션·격리 PostgreSQL 검증
- [x] 실제 워크스페이스에서 권한 재설치·대상 설정 후 신규 공지/완료·취소 확인 (사용자 확인, 과거 오전 9시 발송 정책은 폐기)

## 5. 운영 준비

- [x] 운영 DB 수동 백업 스크립트·가짜 Docker 검증 추가 (CX: pg_dump, 아카이브 검사, SHA-256, 부분 실패 정리·기존 백업 보존)
- [x] CX: backup_run.sh·backup_stop.sh로 한국 시간 03시 systemd 사용자 자동 백업 등록·해제, 중복 등록·로그아웃 지속 조건·예약 해제 및 실패 테스트

- [x] DBeaver용 DB 루프백 바인딩과 전용 SSH 터널·조회 계정 설정 절차 작성 (사용자 승인, 서버 적용은 별도)

- [x] 운영 전용 run.sh·stop.sh·view.sh 제공 (Rootless 확인, DB 보존, 빌드·마이그레이션·재생성, 상태·로그·읽기 전용 DB 조회, 가짜 Docker 테스트)
